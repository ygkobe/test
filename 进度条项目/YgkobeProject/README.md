```shell
npm install --legacy-peer-deps

npm run build --legacy-peer-deps

```


```shell
# 创建数据库
CREATE DATABASE car_manage CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 运行celery
celery -A YgkobeProject worker --loglevel=info --concurrency=2

# 运行debug模式
python manage.py runserver 0.0.0.0:8002


nohup jupyter notebook --ip=0.0.0.0 --port=8888  --allow-root &

```